import { executeIntentBatchSingle } from "./execute-intent-batch-single"
import { executeIntentBatches } from "./execute-intent-batches"

export const engineController = {
  executeIntentBatchSingle,
  executeIntentBatches,
}
